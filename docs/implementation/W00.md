# W00 — Contract foundation

## Current scope

Issues #18–#22 implement W00.1–W00.5: import the governing v1.1 bundle unchanged,
expose the locked contract-development environment, validate every JSON Schema
and registry projection offline, and generate deterministic Protobuf descriptors
and Rust bindings. Canonical requirement mappings, packet ownership, gate and
evidence references, dependency semantics, and generated graph projections are
validated independently at the repository boundary. The public `qualify` app
reruns the contract gate, regenerates every projection in a temporary workspace,
rejects any byte difference, and verifies retained packet evidence.

## Governing requirements

- BLD-001 — Faithful implementation
- BLD-002 — Reproducible public interface
- BLD-004 — Packet evidence
- BLD-009 — No implicit policy
- BLD-010 — Production continuity
- W00 contract and manifest requirements in `contracts/work-packets.yaml`

## Components and files

- Immutable source: `Habitat-OS-Codex-Build-Bundle-v1.1/`
- Byte-identical projections: `CODEX-BUILD-SPEC.md`, `contracts/architecture/`,
  `contracts/proto/`, `contracts/schemas/`, and the root contract registries
- Locked environment: `flake.nix`, `flake.lock`
- Validation entry point: `tools/validate_contracts.py`
- Offline JSON Schema validator: `tools/schema_contracts.py`
- Protobuf generation and ABI validator: `tools/proto_contracts.py`
- Binding contract validator: `tools/validate_contracts.py`
- W00 qualification entry point: `tools/qualify_w00.py`
- Packet evidence: `evidence/work-packets/W00/`
- Negative fixtures: `tests/fixtures/schema-contracts/` and
  `tests/fixtures/proto-contracts/`

## Boundaries and compatibility

The validator treats any source-manifest failure, projected-byte difference,
invalid declared metaschema, unresolved packaged reference, stale generated
binding, unexpected Protobuf formatting difference, or ABI regression as a hard
failure. Schema references resolve only from the in-memory `$id` registry and
never from the network. The governing Protobuf source's extra EOF newline is the
only permitted difference from canonical Buf formatting. This bootstrap
introduces no runtime authority, migrations, ABI, or compatibility policy.
At W00, an executable requirement mapping means a unique governing definition
with schema-valid ownership, criticality, implementation and enforcement
declarations, known gates, objective evidence names, acceptance text, and a live
source locator. Future-packet implementation paths and evidence artifacts are
declarations; W00 does not require those later deliverables to exist prematurely.

## Tests and evidence

- `nix run .#validate-contracts`
- `nix run .#apps.x86_64-linux.qualify`
- `nix flake check --show-trace`
- `python -m unittest discover -s tests -v`
- Negative fixtures prove malformed schemas, missing packaged references, and
  stale generated projections, missing requirement mappings, invalid gates,
  dependency cycles, and an incompatible Protobuf field removal are rejected.
- The packet result records the qualified revision, lockfile digest, commands,
  test counts, requirement coverage, and report digests.

## Rollback and blockers

Rollback removes only the bootstrap projections, flake, and validation wrapper;
the immutable source bundle remains the recovery source. No W00 blocker remains
after clean-checkout qualification passes.
